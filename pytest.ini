[pytest]
asyncio_mode = auto
markers =
    demo: Live tests yang membutuhkan API key Bitget Demo dan env var DEMO_LIVE_TESTS=true.
          Di-skip secara default di CI/CD normal.
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
